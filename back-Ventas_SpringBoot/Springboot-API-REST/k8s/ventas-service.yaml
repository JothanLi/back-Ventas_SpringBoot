apiVersion: v1
kind: Service
metadata:
  name: ventas-service
  namespace: innovatech
spec:
  type: ClusterIP
  selector:
    app: ventas
  ports:
    - name: http
      port: 8080
      targetPort: http
